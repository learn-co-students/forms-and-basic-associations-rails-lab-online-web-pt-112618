<h1>Create a Song</h1>

<%= form_for @song  do |f| %>
  <%= f.text_field :title %>
  <%= f.text_field :artist_name %>
  <%= collection_select :name, Genre.all, :name, :name %>
  <br />

  <%= form_for @song do |f| %>
  <input name="song[artist_name][]' id= 1'">
  <input name="song[artist_name][]">
  <input name="song[artist_name][]">
  <input type="submit" value="Submit">
<% end %>

  <%= f.submit %>
<% end %>
