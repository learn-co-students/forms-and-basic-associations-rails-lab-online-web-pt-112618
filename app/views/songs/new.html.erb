<h1>Create a Song</h1>

<%= form_for @song  do |f| %>
  <%= f.label :title %>
  <%= f.text_field :title %>
  <%= collection_select :title, Song.all, :name, :name %>
  <br />

  <%= form_for @song do |f| %>
  <input name="song[artist_name][]">
  <input name="song[artist_name][]">
  <input name="song[artist_name][]">
  <input type="submit" value="Submit">
<% end %>

  <%= f.submit %>
<% end %>
